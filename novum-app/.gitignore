# Directorios de módulos de Node
node_modules/

# Archivos de entorno (variables de entorno)
# Es CRÍTICO NO SUBIR ESTOS ARCHIVOS A TU REPOSITORIO
backend/.env
frontend/.env

# Salida de compilación del frontend (React)
frontend/build/ # Para Create React App
frontend/dist/  # Para Vite (si usaste Vite)

# Archivos de depuración y logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/
.yarn/
logs/
*.log

# Archivos temporales del sistema operativo y editores
.DS_Store
Thumbs.db
.vscode/ # Ignorar configuraciones de VS Code específicas de usuario
.idea/   # Ignorar configuraciones de JetBrains IDEs

# Archivos generados por el paquete (si aplica)
# Por ejemplo, si tienes alguna herramienta que genera archivos temporales
# temp/
# *.tmp

# Archivos de caché
.parcel-cache/ # Si usas Parcel
.eslintcache
.prettiercache
.cache/

# Dependencias instaladas por Cypress si lo usas
# cypress/videos/
# cypress/screenshots/

# Archivos de base de datos local (si se generaran dentro del proyecto)
# Aunque es poco común para PostgreSQL, si tuvieras un archivo de DB local
# *.sqlite
# *.db

# Ignorar archivos de migración de base de datos si son generados automáticamente y no forman parte del control de versiones
# backend/migrations/*.js # Solo si son generados y no se versionan